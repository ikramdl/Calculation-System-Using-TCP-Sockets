# Calculation-System-Using-TCP-Sockets
A distributed system exclusively using TCP sockets, composed of a multi-threaded TCP calculation server, a TCP client program (C and Java), a logging system storing requests, results, and errors
